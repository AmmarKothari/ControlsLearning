#ifndef _CONSTANTS_H
#define _CONSTANTS_H

const float TICKS_TO_MM = 0.349;
const float WHEEL_BASE = 150.0;

const float SCANNER_DISPLACEMENT = 30.0;
const float MIN_VALID_SCAN_DISTANCE = 20.0;

// struct PointStruct{
//     float x, y;
// };

// extern struct PointStruct Point;
// typedef struct PointStruct point;

// struct Pose{
//     float x, y, theta;
// };

// extern struct Pose Pose;
// typedef struct Pose pose;
#endif
