
#include <vector>
#include "test_utils.h"

int ten() {
  return 10;
}

std::vector<int> tens() {
  std::vector<int> tens;
  tens.push_back(9);
  tens.push_back(9);
  tens.push_back(9);
  return tens;
}
